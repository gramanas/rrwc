#include <QVectorIterator>

#include "outputmanager.hpp"

OutputManager::OutputManager() {
}

void OutputManager::generateOutputs(QVector<OutputTab *> outputTabs) {
    for (const auto& it : outputTabs) {
        it->
    }
}
